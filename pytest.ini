[pytest]
markers =

    ##### createHTML #####

    createHTML: Parámetros esperados por lo que se crea el archivo HTML correctamente.
    errorContentHTML: Parámetros de contenido incorrecto por lo que no se crea el archivo HTML.
    errorPath: Parámetro de ruta incorrecto por lo que no se crea el archivo HTML.
    existsFileInPath: El archivo ha sido creado en la ruta deseada.

    ##### END createHTML #####

  

    successful_connection: Comprueba que la conexión con la base de datos ha sido exitosa y contenga las Keys(diccionario) necesarias para el programa.
    Query_List_Brand: El contenido debe ser una lista de marcas.
    Query_List_Category: El contenido debe ser una de categorias.